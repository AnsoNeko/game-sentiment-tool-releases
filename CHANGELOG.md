# 更新说明

## v0.4.1-AIagent - 2026-09-02

### 作者资产、热度监测与 AI 驾驶舱

- 新增作者资产中心，覆盖作者档案、作品分析、公开证据画像、历史趋势、评分与人工等级、采买批次、交付关联和作者推荐；推荐继续使用全量候选、输入指纹及严格预算约束，并完成 500 作者规模验证。
- 新增 B站作品热度监测、立即检查、延期、停止和移除能力，飞书 Webhook/Secret 继续由 Electron `safeStorage` 加密保存，对 renderer 和模型仅返回脱敏配置状态。
- AI 驾驶舱新增作者资产、作者分析、采买管理、作者推荐和热度监测五组受控工具；作者分析保存、人工确认、采买写入、推荐保存、热度操作和导出均按最新用户消息独立授权，附件和历史消息不能授权。
- 系统设置拆分为 5 个业务分页；普通周报和历史变化报告在切换页面后继续运行，并通过全局任务条显示进度、停止和终态。

### 数据兼容与独立更新频道

- 数据库增量升至显式汇合迁移 v6，按结构同时补齐驾驶舱、热度和作者业务表；继续兼容 v1-v5，不重建或覆盖用户 SQLite。
- AIagent 保持独立 appId、包名、EXE、快捷方式和 `%APPDATA%\toolbox-sentiment-electron-agentver-internal`，只读取 `channels/agentver/latest.json`，不会访问或回退普通版 Latest。
- 本版本固定使用 `v0.4.1-AIagent` Prerelease 和 `GameSentimentTool-AIagent-v0.4.1-win-x64-Setup.exe`，不会替代普通版 `v0.4.1` Latest。

### 验证与校验

- Python compileall、86 项后端测试、迁移汇合、AI/驾驶舱/附件/任务守卫、作者分析、500 作者规模、热度、导出、更新双通道、安装配置、安全、路径、Python runtime、Electron/打包脚本语法、renderer TypeScript 与 Next.js production build全部通过。
- `1440x1000` 与 `768x1000` 隔离界面验收覆盖驾驶舱默认入口、作者中心、热度页、五页签设置和跨页周报全局任务条；页面无横向溢出，console 0 error / 0 warning。
- Windows x64 NSIS、AIagent 包内容/频道隔离门禁和隔离打包应用冒烟通过；包内 FastAPI/OpenAPI 0.4.1、Next、Logo、退出码和端口释放正常。
- Windows EXE 文件/产品版本：`0.4.1`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,524,393` 字节。
- 安装包 SHA-256：`0D304724EF165911D4F8FE0D312DDBE21CF76C9FBDF672615D1C7D73BCB7A601`。

## v0.4.1 - 2026-09-02

### 系统通用设置分页

- 系统通用设置按 AI 生成、作者资产规则、平台接入与提醒、舆情标签、数据管理拆分为 5 个顶部业务分页，减少功能持续增加后形成的单页纵向堆叠。
- 非活动分页仅视觉隐藏并保持 React 挂载，切换分页不会清空未保存表单；分页提供 `tablist/tab/tabpanel` 语义、左右方向键及 Home/End 键切换。
- 768px 窄屏仅在分页栏内部横向滚动，不产生页面级横向溢出；原 Cookie、本地数据、备份与清理能力按业务归入对应分页，保存和清理接口保持不变。

### 周报 AI 任务全局可见

- 普通 AI 周报与历史变化报告跨侧栏导航保持挂载，离开周报页后仍持续接收请求进度、批次阶段、Reasoning 和正文流；返回后已接收正文完整保留。
- 标题栏下新增全局 AI 报告任务条，显示任务类型、标题、阶段、批次、耗时及运行/完成/失败/取消状态，可从任意页面查看或停止任务，不会自动抢占当前页面。
- 完成结果仍写回对应编辑器或变化报告预览；失败或取消不会覆盖原草稿，终态提示在用户查看或主动关闭前保留，终态后可正常启动下一项 AI 任务。

### 验证与校验

- Python compileall、86 项后端测试、AI、作者分析、500 作者规模、AI 长任务、导出、热度、更新、安装路径、安全、userData、Python runtime、Electron/打包脚本语法、renderer TypeScript 与 Next.js production build 全部通过。
- 1440×1000 与 768×1000 隔离 Playwright 覆盖 5 个设置分页、未保存表单状态、键盘/窄屏滚动，以及普通周报和历史变化报告的跨页运行、完成、停止、旧草稿保护与终态后再次启动；页面无横向溢出，console 0 error / 0 warning。
- Windows x64 NSIS、`latest.json`、P0 包内更新门禁和隔离打包应用冒烟通过；包内 FastAPI/OpenAPI 0.4.1、Next、Logo、退出码和端口释放正常。数据库迁移保持 v6，不新增后端业务接口或 AI 凭据格式。
- `v0.4.1` 已发布为普通版 GitHub Latest（非 Draft、非 Prerelease）；安装包和 `latest.json` 均为 `uploaded`，GitHub size/digest 与本地一致。Draft 全新目录回下载的两项文件逐字节一致，中文正文无 BOM、乱码、`?` 或替换字符。
- 发布后真实 Electron 网络检查返回 `latestVersion=0.4.1`、`canAutoUpdate=true`、安装器大小 `98,491,524`。当前机器系统级 v0.4.0 的真实 `app.asar` 随后从正式 Release 非缓存下载并校验完整安装包，完成 `installer-started → completed`、NSIS 退出码 0 和 v0.4.1 自动重启。
- 升级后 EXE/FileVersion/ProductVersion 与 FastAPI/OpenAPI 均为 0.4.1，Next/Logo 正常；稳定 userData SQLite `integrity_check=ok`、迁移 `[1,2,3,4,5,6]`，项目 3、评论 725、报告 4、采集任务 59、作者 15、作品 21 及三份加密配置摘要保持不变。应用正常关闭后 `3000/8765/32123` 全部释放。
- Windows EXE 文件/产品版本：`0.4.1`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,491,524` 字节。
- 安装包 SHA-256：`608DB4AC676CFE975DD292BC09F06310ED0B3DB38FB104AED1FBC239AF88C24C`。

## v0.4.0 - 2026-08-27

### 作者资产中心与采买记录

- 新增独立“作者资产中心”，按平台作者档案分别维护主页事实快照、作品、画像、等级、项目关系和采买历史；同名跨平台作者不会错误合并。
- 支持采买批次、采买项、报价、计划交付和实际交付绑定。金额使用整数分和 CNY，取消记录保留历史；交付只能绑定同平台作者的规范反链作品。
- 反链采集可在 Cookie 预检通过后原子创建任务、主页来源和采买项；失败主页、规范 URL 重复及二阶段同作者冲突均保留可审计状态。

### 后台作品分析、作者画像与评分趋势

- 反链任务完成后可在后台队列中分析作品内容并汇总作者画像；支持 OpenAI、DeepSeek 和 Ollama 结构化输出、逐项失败隔离、取消、仅重试失败、证据查看、人工确认和字段锁定。
- 作者评分只比较同平台有效样本，支持 7/30/90/180 天及自定义趋势区间。数据不足会降低覆盖率，不补零、不合并不可比平台指标。
- 评分配置、标签体系、系统建议和人工最终等级/风险均保留追加式版本；人工锁定结果不会被后续采集或 AI 建议覆盖。

### 采买推荐与规模化优化

- 推荐默认覆盖全部已验证作者，综合标签匹配、受众画像、作者能力、近期趋势、性价比和风险安全六个维度；人工最终等级和风险优先。
- 价格严格按当前批次报价、同项目 180 天报价、365 天已完成成交中位数、待询价四级回退。缺失价格作者可保留为待询价，但不能进入严格预算方案。
- 新求解器 `all-candidate-local-search.v1` 对全部合格有价作者执行精确人数/最低预算可行性判断，再以三个确定性起点做稳定近似优化；约 500 位作者规模下仍支持候选分页、筛选、排除标签和三套方案。
- 推荐预览不写库；显式保存时后端重新计算并校验输入指纹，不创建采买批次、不改变采买状态。历史记录可安全导出 CSV/XLSX，文本字段防止表格公式注入。

### 数据、兼容性与发布验证

- SQLite 增量迁移至 v6，继续兼容 v1-v5；迁移只增加作者资产相关表、约束和索引，不重建或替换真实数据库。
- Python compileall、86 项后端测试、热度、AI/O3、500 作者规模、AI 长任务、导出、更新、安装路径、安全、userData 路径、Python runtime、Electron 语法、renderer TypeScript 与 Next.js production build 通过。
- Windows x64 NSIS、`latest.json`、P0 包内门禁和隔离打包 EXE 冒烟通过。真实 v0.3.5→v0.4.0 更新状态为 `installer-started → completed`，NSIS 退出码 0，新版自动重启；FastAPI/Next/Logo 正常，稳定 userData 业务数量及三份加密配置摘要保持不变，正常关闭后端口释放。
- `v0.4.0` 已发布为普通版 GitHub Latest（非 Draft、非 Prerelease）；安装包与 `latest.json` 均为 `uploaded`，GitHub size/digest 与本地一致，Draft 全新目录回下载的安装包和清单摘要逐项一致，中文正文无 BOM、乱码或替换字符。
- 发布后真实 Electron 网络检查返回 `latestVersion=0.4.0`、`canAutoUpdate=true`、安装器大小 `98,488,428`。官方 v0.3.5 更新模块随后从真实 Release 非缓存下载完整安装包，完成大小/SHA-256 校验、`installer-started → completed`、NSIS 退出码 0 和新版自动重启；SQLite 完整性、业务数量和三份加密配置摘要保持不变。
- Windows EXE 文件/产品版本：`0.4.0`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,488,428` 字节。
- 安装包 SHA-256：`2527833802B0FF99A6CFE2E845F3F4F48EA694FA8D73842C1D2CA30DD3D8EB70`。

## v0.3.5 - 2026-08-26

### B站热度监测

- B站成功反链作品可批量开启热度监测；也可手动添加标准视频链接或 `b23.tv` 短链接，并可选归属项目与监测截止时间。
- 默认从作品发布时间精确监测 72 小时；支持立即检查、停止、精确修改截止时间以及快捷延长 1/3 天。停止或到期任务可确认移除，规范反链作品不会随监测历史一并删除。
- 应用启动后立即检查，之后每 10 分钟读取一次综合热门前 100，并逐个查询活动视频接口展示的“正在看”总人数；应用关闭即停止。
- 在线人数可解析下限达到 1000、或作品进入综合热门前 100 时各提醒一次；同轮同时发生时合并提醒，活动监测结果持续高亮。

### Windows 与飞书提醒

- Windows 系统通知包含项目、作者、视频标题、在线人数/热门名次和触发原因；点击通知只允许打开校验后的 B站 HTTPS 视频链接。
- 系统通用设置支持飞书 V2 自定义机器人 Webhook、最多 10 个自定义关键词和可选签名密钥；富文本提醒带触发原因和可点击视频链接。
- Webhook URL/令牌和签名密钥使用 Electron `safeStorage` 独立加密，不进入 SQLite、日志、导出或 renderer 状态；Webhook 与 Windows 通知独立投递，失败最多重试 6 轮。

### 界面、数据与兼容性

- “反链数据采集”和“B站热度监测”改为反链页面顶部并排分页，只展示当前工作区；桌面表格与窄屏卡片均保留完整操作。
- SQLite 增量迁移至 v5，新增监测与提醒投递表，继续兼容 v1-v4 数据库。反链历史清理、完整备份和彻底抹除已覆盖新表及引用保护。
- 热榜与在线接口独立容错；403/418/429、网络或结构变化只记录诊断，不停止监测，也不会误判 B站 Cookie 失效。

### 验证与校验

- Python compileall、60 项后端测试、热度监测、安全、安装器、路径、Python runtime、更新 smoke、Electron 脚本语法、renderer TypeScript 和 Next.js production build 通过。
- 1440×1000 与 768×900 隔离 Playwright 验证并排分页、手动链接弹窗、停止任务移除和窄屏卡片，console error 为 0；公开 B站视频元数据低频只读解析成功。
- Windows x64 NSIS、`latest.json`、P0 包内更新门禁和隔离打包应用 FastAPI/Next/Logo/退出端口释放通过；本次按用户要求不执行 Release 资产回下载测试。
- `v0.3.5` 已发布为普通版 GitHub Latest（非 Draft、非 Prerelease）；安装包与 `latest.json` 均为 `uploaded`，GitHub size/digest 与本地产物一致，Release 中文正文核对无乱码或替换字符。
- 独立的 `v0.3.4-AIagent` 仍保持 Prerelease，原 AIagent 资产未修改，也未被设为普通版 Latest。
- Windows EXE 文件/产品版本：`0.3.5`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,390,021` 字节。
- 安装包 SHA-256：`0A2804D69B8A407C1EE122D6170F14C3130F8A36A9F4B2CAC2A8A6A1E67FC9F1`。

## v0.3.4-AIagent - 2026-08-25

### AI 驾驶舱与反链采集

- 新增 AI 驾驶舱，支持项目归类持久会话、逐轮模型与思考强度、受控业务工具、附件读取和删除审批；附件不向模型或 renderer 暴露本机路径。
- AI 发起的反链、评论采集、评论分析、报告和导出按用户当轮明确授权分阶段执行；平台 Cookie 需求由后端依据真实主页或作者档案推导。
- 用户自主操作的反链数据采集与普通版 v0.3.4 对齐：首词作为主关键词，按主页执行副关键词兜底，副词候选必须由主关键词精确匹配真实标签后才能保存。

### 独立安装与更新频道

- 产品名和快捷方式为“项目舆情与运营支持工具 AIagent版”，主程序为 `GameSentimentToolAIagent.exe`；与普通版使用不同安装记录及 `%APPDATA%\toolbox-sentiment-electron-agentver-internal` 数据目录。
- AIagent 只读取主分支 `channels/agentver/latest.json`，要求 `channel: "agentver"`，并只接受 `v<version>-AIagent` 标签和 `GameSentimentTool-AIagent-v<version>-win-x64-Setup.exe`。不会访问或回退到普通版 Latest。
- 普通版 v0.3.4 继续保持 GitHub Latest，其 `latest.json` 与原安装资产不修改。两版可并行安装，但因共享 `8765/32123` 端口不能同时运行。

### 验证与校验

- 后端、AI、驾驶舱、附件、任务守卫、反链、导出、更新频道隔离、安全、路径、安装器、Python runtime、Electron 语法、renderer TypeScript 和 production build 全部通过。
- Windows x64 NSIS、app.asar、Next standalone、FastAPI/OpenAPI 0.3.4、AI 驾驶舱资源、独立更新配置及打包应用退出端口释放门禁通过。
- 真实 UAC/NSIS 安装退出码 0；普通版与 AIagent 的安装记录、EXE、快捷方式和 userData 独立。普通版数据库及加密配置摘要未变化，AIagent 数据库 `integrity_check=ok`、迁移为 v5，既有项目、评论、驾驶舱会话、Cookie 和 AI 配置保持可读。
- `v0.3.4-AIagent` 已发布为 Prerelease，两项资产均为 `uploaded`；GitHub size/digest 与本地一致，草稿回下载的安装包和清单 SHA-256 分别完全一致，Release 中文正文无 BOM、乱码或替换字符。
- 发布后真实 Electron 网络检查确认普通版仍返回 `GameSentimentTool-v0.3.4-win-x64-Setup.exe`，AIagent 只返回 `GameSentimentTool-AIagent-v0.3.4-win-x64-Setup.exe`。Agent 更新模块又从真实 Release 非缓存下载完整安装包并通过大小及 SHA-256 校验。
- GitHub `releases/latest` 仍为普通 `v0.3.4`；原普通安装包和 `latest.json` 的资产 ID、大小及 digest 均未变化。
- Windows EXE 文件/产品版本：`0.3.4`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,399,806` 字节。
- 安装包 SHA-256：`84A145419C3E443DCD6EF5DDFC6CC95D03960B9EE1316E5C1FA70B1C7255CC72`。

## v0.3.4 - 2026-08-25

### 反链主、副关键词采集规则

- 多关键词按输入顺序解释：第一个是主关键词，其余是副关键词；规则按每个作者主页独立执行。
- 在日期和扫描上限内，主页只要存在主关键词命中的作品，就只保存主词结果，不启动该主页的副词兜底；主词零结果时，才对全部副关键词候选取并集并去重。
- 副词候选必须由主关键词包含匹配作品真实标签后才会保存。B站仅使用标签接口的真实 tag，不接受分区名；抖音使用 hashtag，小红书使用 `tag_list`。标签缺失或接口失败时拒绝候选，但不把主页标记为平台采集故障。
- 关键词与标签比较统一使用 Unicode NFKC、去首尾空白和大小写不敏感处理。副词有候选但全部未通过验证时记录 `NO_VERIFIED_MATCH` 并保留候选数量，完全没有候选时保持 `NO_MATCH`。
- 历史 ANY/ALL 任务重试时也归一化到新策略；指定作品补采仍绕过关键词和日期规则。没有新增数据库迁移，继续使用 v4 数据库。

### 界面、结果与导出

- 新建任务界面移除 ANY/ALL 选择，按输入顺序标识主关键词和副关键词，并说明逐主页兜底及主词标签验证。
- 任务详情、结果表、CSV 和 XLSX 增加主词、副词、发现方式和验证结果说明；主词结果记录主关键词，兜底结果记录主关键词及实际命中的副关键词。
- `keywordMode` 保留兼容字段名并统一返回 `primary_fallback`；结果发现方式新增 `primary_keyword`、`secondary_fallback`，继续兼容历史 `homepage` 和指定作品 `manual`。

### 验证与校验

- Python compileall、49 项后端测试、AI/长任务、CSV/XLSX 导出、反链导出、更新、安全、路径、Python runtime、Electron 脚本语法、renderer TypeScript 与 Next.js production build全部通过。
- Windows x64 NSIS、打包资源、小红书签名运行时、`latest.json` 及应用内更新 P0 静态门禁通过；隔离打包 EXE 返回 FastAPI/OpenAPI 0.3.4、Next/Logo 200，正常退出后端口释放。
- 本机真实 UAC/NSIS 覆盖安装状态依次为 `installer-started`、`completed`，NSIS 退出码 0，新版主窗口自动拉起；真实 SQLite 只读 `integrity_check=ok`，既有 2 个项目和 586 条评论可读，Cookie、安全设置和 AI 配置文件摘要在安装前后一致。
- 正式 Release 已设为 Latest，两项资产均为 `uploaded`；GitHub digest、Draft 回下载安装包和清单摘要与本地逐项一致，中文正文无 BOM 或乱码。发布后真实 Electron 网络检查返回 `latestVersion=0.3.4`、`canAutoUpdate=true`、`installerSize=98367808`。
- 上一版本 P0 验收由官方 v0.3.3 安装模块从正式 Release 重新下载 v0.3.4（非缓存复用），完成大小/SHA-256 校验、`installer-started → completed`、NSIS 退出码 0 和新版自动重启；升级后的数据库、业务数量及三份加密配置摘要保持不变，正常关闭后进程与端口释放。
- Windows EXE 文件/产品版本：`0.3.4`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,367,808` 字节。
- 安装包 SHA-256：`4C34D5D0FE0A83D1B1B0F0C606FFB257C7269F7C300EE1C6166C6B7BDFD9E3E3`。

## v0.3.3 - 2026-08-20

### P0 应用内更新修复

- 修复从 v0.2.x 延续至 v0.3.2 的灾难性更新体验：点击“重启并安装”后，旧客户端可能先退出，而 PowerShell/NSIS 尚未真正启动，最终既没有安装界面，也没有自动恢复。
- 新链路由附着于旧 Electron 的 bootstrap 请求 Windows 管理员权限，再启动独立的管理员工作进程。工作进程先创建 NSIS 并写入 `installer-started` 状态；只有 Electron 读到该握手后才允许旧客户端退出。
- 管理员工作进程继续等待 NSIS，记录退出码并写入 `completed` 或 `failed`；成功时自动打开新版，安装阶段失败时重新打开原应用。取消 UAC、工作进程提前退出或握手超时时，旧客户端保持运行并显示诊断日志位置。
- 所有参数以 UTF-8/Base64 内嵌到提权命令，避免 Windows UAC 不继承父进程临时环境变量，以及中文或带空格路径导致助手静默退出。

### 强制迁移说明

- **v0.3.2 及更早安装版无法反向获得本次修复，必须从本 Release 手工下载 v0.3.3，并以管理员身份覆盖安装一次。** 安装程序会沿用原路径，稳定 `%APPDATA%\toolbox-sentiment-electron` 数据目录不变。
- 完成这一次手工迁移后，v0.3.3 才是后续“应用内自动更新”的可信基线。

### P0 发布门禁与真实验证

- “应用内更新”升级为每个新版本的 P0 阻断项。打包命令强制检查 `latest.json`、文件大小/SHA-256、Windows 版本资源、NSIS 原路径复用和打包 `app.asar` 中“安装器先就绪、客户端后退出”的顺序；任一失败不得发布。
- 正式安装包完成真实 UAC/NSIS 验收：状态依次为 `installer-started`、`completed`，NSIS 退出码 0，旧进程退出后安装版 0.3.3 主窗口自动拉起；正常关闭后 Electron/FastAPI/Next 进程与端口均释放。
- 隔离打包 EXE 冒烟返回 FastAPI/OpenAPI 0.3.3、Next/Logo 200；真实 SQLite 只读 `integrity_check=ok`，安全设置、Cookie 和 AI 配置文件摘要在覆盖安装前后一致。
- Windows EXE 文件/产品版本：`0.3.3`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,364,883` 字节。
- 安装包 SHA-256：`4777774DAFA2160EB9D709960E0C0ADF26C447D76415D93969C2BACD22A1A537`。
- 正式 Release 已设为 Latest，两项资产均为 `uploaded`；GitHub digest、Draft 回下载安装包/清单 SHA-256 与本地逐项一致。发布后真实 Electron 网络检查返回 `latestVersion=0.3.3`、`canAutoUpdate=true`、`installerSize=98364883`。

## v0.3.2 - 2026-08-20

### 修复与改进

- 修复抖音反链主页作者名可能被作品列表或详情中的昵称覆盖的问题。主页资料接口的当前昵称作为权威值，只有资料名为空且作品作者 ID 与主页一致时才使用作品昵称兜底。
- 反链 CSV/XLSX 导出改为 Windows 系统“另存为”。后端仅生成受控暂存文件，主进程校验目录、扩展名和符号链接后保存到用户选择位置；取消选择会清理暂存文件。
- “采集任务与主页明细”新增总体进度条、完成比例，以及当前作者、平台和主页提示；运行中的主页行增加状态高亮。
- 进度条新增 `HH:MM:SS` 累计任务耗时。运行中每秒更新，成功、部分失败、失败、取消或中断后固定；主页重试、批量失败重试和指定作品补采只累计新的实际运行区间。
- 作者库新增刷新按钮，并明确按平台主页档案分别保存和选择。同名作者的 B站、抖音和小红书主页不会按名称合并，可分别勾选并启动对应平台采集。
- 系统通用设置新增反链历史任务清理：选择保留最近 20/50/100/200/500 条已结束任务，先预览再确认；等待/运行任务、作者库及作者—项目关联始终受保护。

### 数据与安全

- SQLite 增量迁移至 v4，反链任务保存累计秒数和当次运行起点；应用异常退出恢复为“已中断”时会先结算当次时长。旧 v3 终态任务按首个开始时间和最终结束时间执行一次兼容回填。
- 历史任务正式删除会在写事务中重新核对候选数量与状态，并清理不再被任何任务引用的作品；不会删除作者库或作者—项目关系。
- Cookie、安全设置、AI 配置和稳定 `%APPDATA%\toolbox-sentiment-electron` userData 路径保持不变；Cookie 明文不会进入 renderer、SQLite、日志或导出。

### 验证与校验

- 41 项后端测试、AI/长任务、来源归属与反链导出、更新安装助手、安全、路径、Python runtime、Electron 脚本语法、renderer TypeScript 和 Next.js 15.5.20 production build 全部通过。
- Windows x64 NSIS、文件/产品版本、CompanyName、app.asar 模块、小红书签名运行时和 `latest.json` 交叉校验通过。
- 隔离 `win-unpacked` 返回 OpenAPI `0.3.2`、Next/Logo 200、SQLite 迁移 `[1,2,3,4]`，定时退出后 FastAPI/Next 端口释放；真实用户数据库和配置未写入。
- Windows EXE 文件/产品版本：`0.3.2`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,363,192` 字节。
- 安装包 SHA-256：`FB663121613FA6069212CD0D776AD701DB157B6AB3BD33D4A12C19FAE2A4A11B8`。

## v0.3.1 - 2026-08-19

### 新增与改进

- 侧栏新增“反链数据采集”。同一任务可混合手动粘贴的新作者主页和作者库已有作者，并可选关联游戏项目。
- B站、抖音和小红书支持按作者主页、关键词 ANY/ALL、上海时区起止日期和可调扫描数量查找作品，保存作品链接、标题/正文/标签及平台公开互动指标；不采集评论正文、不写入评论池。
- SQLite 增量迁移至 v3，新增作者档案、作者—项目多对多关联、任务来源、规范作品、任务结果和指标快照；作者改名、主页别名、历史任务快照及失败原因均可保留。
- 新建任务、主页重试、批量失败重试和指定作品补采在启动前验证本次涉及平台的 Cookie；缺失、失效或校验受限时不会创建、启动或重置任务。
- 支持任务取消、中断恢复、单主页重试、重试全部失败、指定作品链接补采，以及 CSV/XLSX 导出；指定作品补采仍校验平台、可访问性和已知作者归属。
- 本地平台采集和指定链接采集取消每内容/链接 300 条评论硬上限；超过 300 条显示耗时和平台风控风险提示，但不修改输入或阻止任务。Steam、B站、抖音、小红书和微博采集器不再静默截断。

### 同版本热修复：软件内重启安装

- 修复点击“重启并安装”后应用退出、但 NSIS 没有完成升级且没有错误反馈的问题。更新助手现在会等待旧进程退出，显式显示 Windows 管理员权限确认，并等待、记录安装器退出码。
- 安装成功后通过 `--force-run` 自动打开更新后的应用；用户取消管理员确认或安装器失败时，会重新打开原版本、显示失败提示，并提供 `%APPDATA%\toolbox-sentiment-electron\logs\update-install.log` 日志入口。
- 更新助手脚本使用带 BOM 的 UTF-8，确保 Windows 自带 PowerShell 5 能正确解析中文诊断内容；更新 smoke 覆盖提权、退出码、成功重启、失败恢复和 PowerShell 5 语法。
- 本次按用户要求仍使用 `v0.3.1` 标签并替换同名资产。由于首批 v0.3.1 与此前版本中的旧更新代码无法反向修复，受影响用户需要手动下载刷新后的 v0.3.1 安装包并以管理员身份覆盖安装一次；同版本不会再次出现自动更新提示。

### 数据与安全

- 保留稳定 `%APPDATA%\toolbox-sentiment-electron` userData 路径；v1/v2 数据库增量迁移到 v3，不重建或替换既有项目、评论、周报及设置。
- Cookie 明文仍只存在于 Electron IPC 和当次采集内存，不进入 renderer、SQLite、日志、作者库、备份或导出。
- 不绕过验证码、登录风控、付费墙或访问限制，不执行发布、点赞、评论、私信、上传、关注等自动互动。

### 验证与校验

- 后端 36 项、AI、AI 长任务、导出、更新、安全、安装路径、稳定 userData、Python runtime、Electron 脚本语法和 renderer TypeScript 全部通过。
- Next.js 15.5.20 production build、Windows x64 NSIS 和打包资源检查通过；隔离 `win-unpacked` 的 FastAPI/OpenAPI `0.3.1`、Next 页面、SQLite v1/v2→v3 迁移及退出端口释放通过。
- 打包 Electron Node 模式下的小红书签名自检通过；安装资产和 `latest.json` 的版本、大小、URL 与 SHA-256 一致。
- Windows EXE 文件/产品版本：`0.3.1`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,356,338` 字节。
- 安装包 SHA-256：`48B1B375018923EE44ECBCB93090239C8A26FE73E39F2DA47100DED6CA9B9EAE`。

## v0.2.11 - 2026-07-28

### 修复

- 修复 v0.2.10 点击“检查更新”时报 `TypeError: net.fetch is not a function`；主进程现在明确使用 Electron/Chromium `net`，并在启动时校验网络接口。
- 检测到旧版本时自动读取并沿用原安装路径，不再要求升级用户重新选择目录；全新安装使用默认目录。
- 软件内下载、固定仓库限制、文件大小及 SHA-256 校验、退出后静默覆盖安装和稳定 userData 兼容策略保持不变。

### 迁移说明

- v0.2.10 的更新检查无法使用，因此需要从本 Release 手动安装 v0.2.11 一次；安装后后续版本可继续在软件内直接更新。

### 校验

- 安装包大小：`98,319,141` 字节。
- 安装包 SHA-256：`03F3BE1B95550D414D3083DF4099499DF3417916CC209D3E6F20EC08B5CE6C95`。
- Authenticode：`NotSigned`。

## v0.2.10 - 2026-07-28

### 修复

- 更新检查与安装包下载改用 Electron/Chromium 系统网络通道，兼容 Windows 系统代理。
- 修复 v0.2.9 在部分代理网络中 Node 直连 GitHub Release 资产失败、错误退化为打开网页的问题。
- 真实 Electron 网络测试已读取公开 `latest.json`，确认可直接更新、安装资产名称和大小完整。

### 校验

- 安装包大小：`98,325,720` 字节。
- 安装包 SHA-256：`BCA5BC27F969D798A782B43B634D6C97FB12F0E977C7CCA676DF62BFACB5F91F`。
- Authenticode：`NotSigned`。

## v0.2.9 - 2026-07-28

### 新增与改进

- 关于页新增软件内直接更新：发现后续版本后可直接下载安装包、查看实时进度、取消下载，并在校验完成后点击“重启并安装”。
- 更新安装包由 Electron 主进程下载；renderer 不提供任意下载地址。只接受固定公开 Release 仓库、规定的 Windows x64 安装包名称及有效版本。
- 下载完成后强制核对安全清单声明的文件大小与 SHA-256；不一致的文件会被删除并拒绝安装，已完整校验的文件可复用。
- 安装助手等待当前应用及本地服务退出后，再使用 NSIS 更新参数覆盖安装；项目、评论、周报、Cookie、安全设置和 AI 配置继续保存在稳定 userData 目录。
- 发布资产新增 `latest.json`。后续每个版本必须同时上传安装包与安全清单，软件内更新不依赖 GitHub REST API 配额。
- Release 发布说明入口继续保留，在清单缺失或网络异常时提供人工兜底。

### 迁移说明

- v0.2.8 及更早安装版没有软件内下载代码，因此仍需从本 Release 手动安装 v0.2.9 一次。
- 安装 v0.2.9 后，未来带 `latest.json` 的版本即可在软件内完成下载和安装。

### 验证与校验

- 更新 smoke 覆盖清单校验、固定仓库限制、直接下载、进度、取消清理、大小/SHA-256 拒绝、校验文件复用和退出后安装助手。
- 后端 16 项、AI、AI 长任务、导出、安全、路径、Electron userData、Python runtime、Electron 脚本语法、TypeScript 和 Next production build全部通过。
- Playwright 桌面与 768px 验证检查更新、下载、100% 校验完成及重启安装动作，无横向溢出。
- Windows x64 NSIS、FastAPI/OpenAPI `0.2.9`、Next 页面和退出端口释放验证通过。
- Windows EXE 文件/产品版本：`0.2.9`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,325,325` 字节。
- 安装包 SHA-256：`493B24563A987873C6834AB1C37C5CAD6CE6BD38FB93FCA4A1CC1693B4E97B4A`。

## v0.2.8 - 2026-07-28

### 新增与改进

- Steam 评论现在优先采用平台原始“推荐/不推荐”作为正面或负面结论；仅在平台没有提供明确评价时，才使用本地脚本识别情感，避免叙事复杂的正面长评被关键词误判为负面。
- 评论情感会记录平台判定、脚本判定或人工修订来源；历史 Steam 数据会执行一次兼容校正，同时保留人工修订结果，不覆盖运营人员已经确认的结论。
- “舆情事件深度研判”在桌面端改为随页面滚动保持可见，在窄屏端使用固定抽屉，编辑列表下方评论时不再需要回到页面顶部。
- 总览仪表盘、标签统计、周报/历史变化报告和运营数据导出默认按评论采集时间筛选、分组及统计；页面可切换为评论发布时间，用于观察评论实际发布节奏。
- 评论池与导出明细同时保留“评论发布时间”和“采集时间”，便于识别补采旧评论；AI 报告上下文会明确标注当前时间口径。
- 保留 v0.2.7 的指定链接失败重试、v0.2.6 的 Python 自动准备及稳定历史 userData 路径，无需迁移项目、评论、周报、Cookie、安全设置或 AI 配置。

### 使用方法与注意事项

1. 正常退出旧版本后运行 `GameSentimentTool-v0.2.8-win-x64-Setup.exe` 覆盖安装，无需卸载或迁移数据。
2. Steam 新采集评论会直接沿用平台推荐状态；历史记录在首次使用新版本时自动完成一次兼容校正，人工修订不受影响。
3. 统计和报告默认使用“采集时间”；如需按玩家实际发评日期分析，可在对应页面切换为“评论发布时间”。
4. 安装器未进行 Authenticode 代码签名，可能触发 Windows SmartScreen；请只从本仓库 Release 下载并核对 SHA-256。

### 验证与校验

- 后端 16 项测试、AI、AI 长任务、导出、更新、安全、稳定路径、Electron userData、Python runtime、Electron 脚本语法和 TypeScript 全部通过。
- Next.js production build 与 Windows x64 NSIS 打包完成；打包后 FastAPI/OpenAPI 版本为 `0.2.8`，独立 userData 数据库路径正确。
- 打包应用实际启动验证通过：同一测试集默认按采集时间统计为 2 条，切换评论发布时间后为 1 条；页面无控制台警告或错误。
- 应用正常退出后 `8765/32123` 端口均已释放。
- Windows EXE 文件/产品版本：`0.2.8`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,321,703` 字节。
- 安装包 SHA-256：`F2C667FD09EFF0DA80EAF7F51F3390227846570BC59236E31A087B8F650554C0`。

## v0.2.7 - 2026-07-16

### 新增与改进

- “指定链接采集”在批量任务部分失败后新增“重试全部失败（N）”，无需从大量原始链接中手工查找、复制和重新提交失败项。
- 每个失败结果行新增单条“重试”；成功、重复和无法识别的链接不会进入失败重试集合，也不会被重复采集。
- 批量重试继续按原链接顺序低频串行执行，只更新目标失败行；重试成功后转为“已完成”，再次失败则保留最新失败原因。
- 首轮目标项目、每链接评论上限和 Steam 语种/日期条件保存为当前页面的批次快照；用户随后修改表单时，失败重试仍沿用首轮配置，避免写入错误项目。
- 结果区实时汇总完成、失败、处理中和跳过数量；全量重新采集保留为次要操作，编辑链接或刷新/重启会清空当前页面失败集合。
- 完整保留 v0.2.6 的 Python 自动准备、稳定历史 userData 路径、SQLite v2、分平台 Cookie、安全设置和 AI 配置兼容。

### 使用方法与注意事项

1. 正常退出旧版本后运行 `GameSentimentTool-v0.2.7-win-x64-Setup.exe` 覆盖安装，无需卸载或迁移数据。
2. 指定链接批次结束后，如存在失败项，可点击底部“重试全部失败（N）”，或点击对应失败行右侧的“重试”。
3. 重试会沿用首轮目标项目和采集参数；如希望使用当前表单的新参数，请选择“重新采集全部链接”。
4. 重试状态只保留在当前页面会话，应用不会自动循环重试，也不会绕过验证码、登录风控或访问限制。
5. 安装器未进行 Authenticode 代码签名，可能触发 Windows SmartScreen；请只从本仓库 Release 下载并核对 SHA-256。

### 验证与校验

- 部分失败、批量只重试失败、单条重试、持续失败、项目/参数快照及 1440/768px 界面回归通过。
- 后端 13 项、AI、AI 长任务、来源归属导出、更新、安全、稳定路径、Electron userData、Python runtime、Electron 脚本语法和 TypeScript 全部通过。
- Next.js production build 与 NSIS 完成；打包 renderer 包含新重试界面，隔离 `win-unpacked` 的 FastAPI `0.2.7`、Next 首页和退出端口释放验证通过。
- Windows EXE 文件/产品版本：`0.2.7`；CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,317,853` 字节。
- 安装包 SHA-256：`803BF5D9B5EA85EB5618E196132E5D0A171A899656DDB43F9912A494273ED64B`。

## v0.2.6 - 2026-07-15

### 修复与改进

- 修复部分用户电脑没有 Python、命中 Microsoft Store 别名或缺少 FastAPI/uvicorn 依赖时，Next 界面可以打开但本地数据服务未启动，最终被误报为“数据库读取故障 / Failed to fetch”的问题。
- 安装版启动时自动检测 Python 3.10–3.12；没有兼容环境时先征求用户同意，同意后优先通过 WinGet 为当前用户安装 Python 3.12。
- WinGet 不可用或安装后仍未发现 Python 时，从 python.org 下载固定的 Python 3.12.10 官方安装包，校验 SHA-256 后执行当前用户安装。
- 后端依赖安装到 `%APPDATA%\toolbox-sentiment-electron\runtime\python-venv` 隔离环境，不修改用户现有 Python 包；requirements 未变化时二次启动直接复用。
- FastAPI 与 Next 健康检查通过前不打开主工作台；用户取消、Python 安装失败、依赖失败、端口占用和服务启动失败分别提示并提供日志位置。
- 前端不再把网络层 `Failed to fetch` 统一描述为数据库损坏，改为提示“本地数据服务连接失败”。

### 使用方法与注意事项

1. 正常退出旧版本后运行 `GameSentimentTool-v0.2.6-win-x64-Setup.exe` 覆盖安装，无需卸载或迁移数据。
2. 首次启动如未检测到兼容 Python，应用会说明安装来源与范围；只有用户点击“同意并安装”后才会自动安装。
3. 首次准备隔离环境需要联网下载 Python 或后端依赖，可能持续数分钟；后续启动将复用已验证环境。
4. 默认继续读取 `%APPDATA%\toolbox-sentiment-electron`。不要手工复制、合并或删除历史 userData 目录。
5. 安装器未进行 Authenticode 代码签名，可能触发 Windows SmartScreen；请只从本仓库 Release 下载并核对 SHA-256。

### 验证与校验

- Python runtime 检测、真实临时虚拟环境、二次复用、用户取消和模拟 WinGet 自动安装测试通过。
- 后端 13 项、AI、AI 长任务、来源归属导出、更新、安全、稳定路径、Electron userData、Electron 脚本语法、TypeScript 和 Next production build 均通过。
- NSIS 覆盖安装后，真实稳定 userData 可读取既有项目、评论、报告与采集任务；数据库迁移仍为 `[1,2]`，退出后 `8765/32123` 端口释放。
- Windows EXE CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,316,488` 字节。
- 安装包 SHA-256：`4D5749C499554533CC36F2A3772B7C145218A19E3CCABA623A0547B8C47E2E69`。

## v0.2.5 - 2026-07-14

### 修复与改进

- 修复评论较多时 AI 周报或历史变化报告容易被固定 5 分钟计时取消的问题：改为持续活动空闲超时并保留独立最长时限，网络分片、分批完成和最终综合进度都会刷新活动期限。
- 用户主动停止、长时间无响应和达到最长时限现在使用不同原因与提示；原周报或上一版变化报告在取消、断流和失败时继续保留。
- AI 生成面板新增已运行时间、分批进度和进度条，便于判断大批量评论分析仍在正常推进。
- 运营数据导出中心的评论明细 CSV、Markdown、JSON 均补齐“来源归属”；统计汇总新增来源归属声量分布。后端 CSV/XLSX 原有来源归属继续保留。
- 完整保留 v0.2.4 的历史变化报告、评论池顶部分页与滚动保持、任务栏图标和稳定 userData 路径修复。

### 使用方法与注意事项

1. 正常退出旧版本后运行 `GameSentimentTool-v0.2.5-win-x64-Setup.exe` 覆盖安装；无需卸载或迁移数据。
2. 评论较多时可在 AI 面板观察运行时间和批次进度；只要模型仍返回数据或批次进度，任务就不会因固定总时长被中止。
3. 若模型在空闲期限内没有任何网络或批次活动，任务会以“长时间无响应”停止；用户仍可点击“停止”主动取消。
4. 导出评论明细后，可在 CSV 的“来源归属”列、Markdown 的“来源归属”条目或 JSON 的 `sourceOwner` 字段中核对来源。
5. 远程 AI 服务会收到本次选择的评论或报告正文，但不会收到 Cookie、API Key、安全设置或本地路径；本工具仍不绕过平台验证码和风控。

### 验证与校验

- 后端 13 项测试、AI 流式/多批次、长任务守卫、来源归属导出、更新、安全和 userData 路径测试全部通过。
- Next.js production build、隔离 Playwright 页面导出与 AI 批次进度回归通过；284 条旧库副本的三种评论明细导出均含非空来源归属。
- `win-unpacked` 与覆盖安装版均读取旧数据库隔离副本中的 2 个项目、284 条评论；FastAPI `0.2.5`、Next 和 Logo 正常，退出后 `8765/32123` 释放。
- 覆盖安装版窗口大/小/Small2 及窗口类图标句柄全部有效；桌面与开始菜单快捷方式继续指向既有安装目录。
- Windows EXE CompanyName：`安索Anso`；Authenticode：`NotSigned`。
- 安装包大小：`98,310,406` 字节。
- 安装包 SHA-256：`1FC32D3C1C706CBA8B1891E0AEC747101F5EBEE2D101A6598457679449E8E650`。

## v0.2.4 - 2026-07-14

### 兼容修订

- 2026-07-14 重新打包并覆盖 v0.2.4 安装资产：修复初版 v0.2.4 设置中文产品名后，Electron 默认 userData 从历史目录 `%APPDATA%\toolbox-sentiment-electron` 切换到中文产品名目录、进而显示空数据库的问题。
- 旧项目、评论、周报、Cookie、安全设置和 AI 配置没有丢失。修订包把产品显示名与存储路径解耦，默认恢复读取历史目录，并继续保留任务栏图标修复。
- 已安装初版 v0.2.4 的用户需要重新下载同名安装包并覆盖安装；由于版本号未变化，应用内更新检查不会提示此次修订。无需也不要手工复制、合并或删除 userData 目录。

### 新增与改进

- 周度通报新增“历史变化报告”：可同时选择多份历史草稿，也可添加多份手工粘贴报告；最新报告支持自动引用、指定草稿或手工粘贴。
- 历史变化报告使用现有 OpenAI、DeepSeek 或 Ollama 配置完成纵向对比，输出变化摘要、指标趋势、问题演化、渠道主题、风险变化、行动建议和依据局限。
- 变化报告支持连续多轮调整，每轮均基于上一版生成完整修订稿，并可编辑、预览、保存到历史草稿、复制或导出 Markdown。
- 玩家评论舆情池修复提名/取消周报引用后回到页面顶部的问题；搜索工具栏下方新增与底部同步的顶部分页。
- Windows 任务栏图标链路增强：应用在窗口创建前设置稳定 AppUserModelId，并在创建和页面加载后显式绑定多尺寸应用图标。
- 完整保留 v0.2.3 的五平台只读采集、Cookie 兼容、来源归属、条件清理、Steam 五语与日期、AI 周报和更新检查能力。

### 使用方法

1. 下载 `GameSentimentTool-v0.2.4-win-x64-Setup.exe`，先正常退出旧版本，再运行安装程序覆盖安装；原有 userData 无需迁移或清理。
2. 在“舆情采集与评论池”使用顶部或底部分页；提名/取消周报引用后当前浏览位置会保持。
3. 在“周度通报自动化生成 → 历史变化报告”选择历史材料与最新报告来源，点击 AI 生成初版后，可在下方连续输入调整要求。
4. 变化报告仍使用“系统通用设置”中当前激活的 AI 配置；远程模型会收到本次选择的报告正文，不会收到 Cookie、安全设置、API Key 或本地路径。

### 注意事项

- 历史报告中的文字仅作为待分析数据，不会作为系统指令执行；AI 仍可能出现判断偏差，请在正式使用前核对原始报告、数值和引用依据。
- 本工具继续只执行低频只读采集，不绕过验证码、登录风控或访问限制，也不自动发布、点赞、回复、私信或关注。
- 本安装包未进行 Authenticode 代码签名，Windows SmartScreen 可能提示未知发布者，请只从本仓库下载并核对 SHA-256。

### 验证与校验

- 后端 13 项测试、AI/更新/安全 smoke、Electron 脚本语法、TypeScript 和 Next production build 均通过。
- NSIS 覆盖安装后，FastAPI `0.2.4`、Next 页面和 Logo 均正常，退出后 `8765/32123` 端口释放。
- Windows EXE CompanyName 为“安索Anso”；窗口大图标、小图标和窗口类图标句柄均已安装态实测为有效值。
- 使用真实历史数据库的隔离副本验证修订安装版可读取 2 个项目、284 条评论；真实数据库未写入。
- Node/Electron 路径测试确认默认 userData 固定为 `%APPDATA%\toolbox-sentiment-electron`，打包内主进程与修复源码哈希一致。
- 安装包大小：`98,308,662` 字节。
- 安装包 SHA-256：`A6FEAD6864FD254771A7CB050C5A8EEF54B2EC9043EC94A0A82BEAA76CADC588`。

## v0.2.3 - 2026-07-13

### 修复与改进

- 小红书主页账号 ID 不再被误识别为单笔记；主页采集会先读取最新笔记，再按配置读取评论。
- 小红书主页和评论请求加入基于完整请求路径的 `x-s`、`x-t`、`x-s-common` 动态签名。
- 抖音主页作品请求加入 `a_bogus`，评论请求加入 `X-Bogus`，并同步 Cookie 中的 `msToken` 与浏览器参数。
- 微博会根据 Cookie 类型选择桌面或移动端接口；有效的桌面 Cookie 不再因为移动端未登录状态被误报失效。
- 抖音、小红书和微博使用持久请求会话；401/明确登录失效与 403/418/429、网页验证、签名失败及平台风控分别提示。
- 开发者名称统一调整为“安索Anso”，同步应用关于页和 Windows EXE CompanyName。
- 完整保留 v0.2.2 的来源归属、条件清理、Steam 五语与日期、按勾选章节生成 AI 周报及更新检查优化。

### 使用方法

1. 下载 `GameSentimentTool-v0.2.3-win-x64-Setup.exe`，关闭正在运行的旧版本后执行安装；可直接覆盖 v0.2.1、v0.2.2 或较早的本地 v0.2.3。
2. 原有项目、评论、周报、Cookie、安全设置和 AI 配置仍保存在当前 Windows 用户目录，不需要卸载旧版或清理用户数据。
3. B站、抖音、小红书和微博采集前，请在对应网站浏览器登录账号，然后把该平台最新 Cookie 单独保存到软件设置页；不要混用不同平台 Cookie。
4. 在“游戏项目管理”配置平台账号主页；在“舆情采集与评论池 → 本地平台采集”选择项目、平台、内容数量和单内容评论数量后开始采集。
5. Steam 商店评价可按日期和简中、英语、日语、俄语、韩语多选，默认仅简体中文，多语种共用采集总量上限。
6. AI 周报生成前先在系统通用设置配置模型与系统 Prompt，再勾选本次需要生成的章节；未勾选的章节不会生成。
7. 舆情池条件清理应先预估命中数并核对筛选条件；建议在正式清除前先从导出中心备份。

### 注意事项

- 本工具只执行低频只读采集，不会绕过验证码、登录风控或访问限制，也不会自动发布、点赞、回复、私信或关注。
- 若平台提示验证码或风控，请先在浏览器中人工确认账号可正常访问，再稍后重试；403/418/429 不一定代表 Cookie 已过期。
- 舆情池条件清理不可撤销；“一键彻底抹除系统大库”的影响范围更大，请勿混淆。
- 使用 OpenAI 或 DeepSeek 需要自行提供 API Key 并承担服务费用；AI 请求会向当前模型服务发送所选评论内容，但不会发送 Cookie、安全设置或本地路径。
- 安装包未进行 Authenticode 代码签名，Windows SmartScreen 可能提示未知发布者。请只从本仓库 Release 下载并核对 SHA-256。

### 验证与校验

- 后端 13 项测试、AI/更新/安全 smoke、Electron 脚本语法、TypeScript 和 Next production build 均通过。
- 使用真实加密 Cookie 的临时副本低数量验证了抖音、小红书和微博采集，未写入真实数据库。
- 隔离 userData 启动验证 FastAPI `0.2.3`、Next HTTP 200、自动退出及监听端口释放。
- Windows EXE CompanyName：`安索Anso`。
- 安装包 SHA-256：`13CDCC923D67D889081B7DDAEA9359FCA563745482D5D61D59AF82151FA38B9C`。

## v0.2.2 - 2026-07-13

### 新增与改进

- 舆情池新增“来源归属”，用于记录评论来自哪位作品作者或投稿人的内容；支持平台自动提取、项目主页覆盖、详情人工修改、筛选和导出。
- 系统通用设置新增“舆情池数据清理”，支持按项目、平台、日期、来源归属、情绪、风险、处理状态、周报状态和关键词组合预估及清理。
- Steam 商店评价新增时间范围和五语多选，支持简体中文、英语、日语、俄语和韩语，默认仅选择简体中文。
- AI 周报严格按页面勾选的章节生成，并将全局系统 Prompt 同时应用于每批分析和最终综合。
- AI 分析上下文新增来源归属统计，可比较不同投稿人的评论量、情绪、风险、标签、点赞和回复表现。
- 更新检查优先解析 GitHub 公开 Release 网页跳转，并增加 10 分钟缓存；未认证 API 配额耗尽时仍可检查最新版本。
- 更新错误提示可区分访问频率受限、网络超时、无公开 Release 和无效版本标签。

### 使用方法

1. 新用户运行 NSIS 安装程序，可选择安装目录并创建桌面或开始菜单快捷方式。
2. v0.2.1 用户可直接安装 v0.2.2。原项目、评论、周报、Cookie、安全设置和 AI 配置继续保存在当前 Windows 用户目录中，启动后会自动执行兼容迁移。
3. 在“游戏项目管理”中，可为平台主页填写来源归属覆盖值；留空时由支持的平台自动识别作品作者。
4. 在“舆情采集与评论池 → 本地平台采集”选择 Steam 后，可设置日期范围和评价语种；所选语种合计共用评论数量上限。
5. 在“系统通用设置 → 舆情池数据清理”中先组合筛选并点击“预估命中数”，核对数量、输入页面要求的确认文字后才可执行清理。
6. 在“周度通报自动化生成”中勾选需要的章节后再点击 AI 生成；不同来源归属的统计会随评论数据一起进入分析上下文。

### 注意事项

- 舆情池条件清理不可撤销。它只删除命中的评论及直接关联映射，保留项目、采集历史和既有周报正文；执行前仍建议导出备份。
- “一键彻底抹除系统大库”的范围更大，会删除项目、评论和周报，请勿与条件清理混淆。
- Steam 多语种采集结果会去重并共同受总量上限约束；日期按本机时区计算并包含起止当天。
- B站、抖音、小红书和微博采集仍需要用户保存有效 Cookie；本工具不会绕过验证码、登录风控或平台访问限制。
- 使用 OpenAI 或 DeepSeek 需要自行配置有效 API Key，并承担对应服务费用；使用 Ollama 前需启动本机或自定义地址的 Ollama 服务。
- AI 请求会将所选项目周期内的评论内容发送给当前激活的模型服务商，但不会发送 Cookie、API Key、安全设置或本地路径。
- 本版本安装包未进行 Authenticode 代码签名，Windows SmartScreen 可能提示“未知发布者”。请只从本仓库 Release 下载，并核对 SHA-256。

### 验证与校验

- 已验证旧 v1 数据库副本增量迁移，原有 2 个项目、130 条评论和加密配置保持兼容。
- 已完成后端、AI、更新、安全 smoke、TypeScript、production build、桌面/768px 界面和 NSIS 隔离安装验收。
- 安装包 SHA-256：`5849838E3470A7FC0A4FC088CA2D2CE43B9650F5086BAAD64730A3E12F0D600A`。

## v0.2.1 - 2026-07-10

### 改进

- Windows 发布物由 Portable EXE 改为 NSIS 安装程序，避免每次启动时执行 portable 自解压流程。
- 安装程序支持选择安装目录，并创建桌面和开始菜单快捷方式。
- 应用代码改用 `asar` 归档，Python 后端和 Next 运行资源按安装目录稳定部署。
- 应用窗口、任务栏、安装器、卸载器、网页 favicon 和工作台标题栏统一替换为新版 Logo。
- 应用版本统一升级为 `0.2.1`。

### 验证

- 已完成静默安装、安装后启动、FastAPI 健康检查、Next 页面与 Logo 请求、静默卸载测试。
- 安装后前后端约 4.9 秒进入可用状态，退出后本地服务端口正常释放。
- 安装包 SHA-256：`8A0E76B7E7318CB2AA678A7E17947C2EC210F99963EF55901F1E69FCD64BCD4D`。

### 签名说明

- 构建流程已启用 electron-builder 证书自动发现，应用、卸载器和安装器均进入自动签名流程。
- 本次公开安装包的构建机器未配置代码签名证书，因此 Authenticode 状态仍为未签名，可能触发 SmartScreen 提示。

## v0.2.0 - 2026-07-10

### 新增

- 更新仓库地址已内置，关于页无需再手动填写 GitHub 仓库。
- 玩家评论舆情池新增服务端翻页，每页最多 100 条，默认展示最新 100 条。
- 玩家评论舆情池新增自动化批量处理，可按当前筛选范围重算情绪、风险和自动标签并标记为已处理。
- 系统通用设置新增 OpenAI、DeepSeek、Ollama AI 配置档管理。
- 支持保存多个 API Key 配置档，并指定一个全局默认配置和模型。
- 支持读取服务商模型、自定义服务地址、全局系统 Prompt 和 Think/Reasoning 模式。
- 周度通报新增“AI 智能分析生成”，支持流式 Markdown、思考摘要展示、取消和超长评论自动分批汇总。

### 安全与兼容

- API Key、AI 系统 Prompt、Cookie 和锁屏安全配置均使用 Windows 系统加密能力保存在本机。
- AI 思考内容仅在生成过程中展示，不写入周报或数据库。
- 保留原规则模板周报功能，现有项目、评论、标签和周报数据无需迁移。

### 注意事项

- 使用 OpenAI 或 DeepSeek 前，需要用户自行配置有效 API Key 并承担对应服务费用。
- 使用 Ollama 前，需要本机或自定义地址的 Ollama 服务已启动并安装模型。
- 当前 Windows Portable EXE 未进行代码签名，可能触发 SmartScreen 提示。
